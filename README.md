# Mern-AuthKit  
A full-stack authentication solution built with the MERN stack, featuring secure login, email verification with OTP, JWT-based auth, protected routes and role-based access.

## 🚀 Features  
- 🔐 User registration and login  
- 🔑 Password hashing with bcrypt  
- ✉ Email verification (OTP) and resend capability  
- 🎯 JWT authentication  
- 🔒 Role-based protected API & UI routes  
- 🧠 Persistent sessions using HTTP-Only cookies  
- 🛠 React Context API for state management  
- 📡 Axios interceptors for token handling  
- ✅ Clean folder structure: `client/` and `server/`  

## 🧰 Tech Stack  
- **Frontend**: React, Tailwind CSS (or your UI styling), Axios  
- **Backend**: Node.js, Express, MongoDB, JWT, Nodemailer  
- **Database**: MongoDB  
- **Auth**: bcrypt (password hashing), JWT (token), OTP via email  
